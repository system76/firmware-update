[package]
name = "boot"
version = "0.1.0"

[lib]
name = "boot"
path = "src/main.rs"
crate-type = ["staticlib"]

[dependencies.compiler_builtins]
git = "https://github.com/rust-lang-nursery/compiler-builtins"
features = []

[dependencies]
alloc_uefi = { path = "alloc_uefi" }
orbclient = { path = "orbclient" }
uefi = { path = "uefi" }
